# Телеграм-бот-напоминалка о приёме Анаприлина

Небольшой бот на Python, который трижды в день напоминает выпить таблетку Анаприлина, ждёт подтверждения и фиксирует историю в `data/confirmations.json`.

## Возможности

- Напоминания в заданные времена (`REMINDER_TIMES`, по умолчанию `12:00,17:00,22:00`).
- Милые напоминания в зависимости от времени суток: «Выпила сегодня утром?», «Выпила сегодня днем?», «Выпила сегодня вечером?».
- Если не подтвердили приём таблетки, бот присылает напоминание «Не забудь ответить!» каждые 10 минут в течение часа (максимум 6 напоминаний).
- Кнопки «Выпила» и «Пропустить» для подтверждения приёма.
- Календарь с цветовой индикацией по неделям:
  - ⚫ Черный — 0 таблеток
  - 🔴 Красный — 1 таблетка
  - 🟡 Желтый — 2 таблетки
  - 🟢 Зеленый — 3 таблетки
  - Кнопки навигации для переключения между неделями
- Команды:
  - `/start` — подписаться на напоминания
  - `/status` — показать состояние напоминаний за сегодня
  - `/calendar` — календарь с статистикой по неделям (с кнопками навигации)
  - `/test` — отправить тестовое напоминание для проверки
  - `/stop` — отключить напоминания для чата
- Все отметки и подписки сохраняются локально в JSON-файлах:
  - `data/confirmations.json` — история приёма таблеток
  - `data/subscribers.json` — список подписанных чатов
- После перезапуска бота подписки и статистика сохраняются — не нужно заново нажимать `/start`.
- Работает в любом чате, где введена команда `/start`.

## Быстрый запуск локально

1. **Склонируй репозиторий:**
   ```powershell
   git clone https://github.com/твой-username/anaprilin-bot.git
   cd anaprilin-bot
   ```

2. **Скопируй `.env` из примера:**
   ```powershell
   copy env.example .env
   ```

3. **Заполни `.env`:**
   - `BOT_TOKEN` — получи токен у [@BotFather](https://t.me/BotFather) в Telegram
   - `TIMEZONE` — часовой пояс IANA, например `Europe/Moscow`
   - `REMINDER_TIMES` — список `HH:MM` через запятую (например `12:00,17:00,22:00`)

4. **Создай виртуальное окружение и установи зависимости:**
   ```powershell
   python -m venv .venv
   .\.venv\Scripts\activate
   pip install -r requirements.txt
   ```

5. **Запусти бота:**
   ```powershell
   python bot.py
   ```

6. **В Telegram отправь боту `/start`** — готово! 💕

## Загрузка на GitHub (для деплоя)

1. **Создай репозиторий на GitHub:**
   - Зайди на [github.com](https://github.com) → New repository
   - Назови `anaprilin-bot` (можно приватный)

2. **Инициализируй git и запуш код:**
   ```powershell
   git init
   git add .
   git commit -m "Initial commit: Anaprilin reminder bot for Liza 💕"
   git branch -M main
   git remote add origin https://github.com/твой-username/anaprilin-bot.git
   git push -u origin main
   ```

3. **Готово!** Теперь можно клонировать на любой сервер.

**Важно:** `.env` и `data/` не попадут в GitHub благодаря `.gitignore` — твои секреты в безопасности! 🔒

---

## Развёртывание на Яндекс.Облаке (рекомендуется)

### Вариант 1: Compute Cloud (виртуальная машина)

1. **Создай виртуальную машину:**
   - Зайди в [Yandex Cloud Console](https://console.cloud.yandex.ru/)
   - Compute Cloud → Создать ВМ
   - Выбери минимальную конфигурацию:
     - ОС: Ubuntu 22.04 LTS
     - vCPU: 2 (5% гарантированная доля)
     - RAM: 1 ГБ
     - Диск: 10 ГБ
   - Публичный IP: включи (для SSH доступа)
   - Стоимость: ~500₽/месяц

2. **Подключись по SSH:**
   ```bash
   ssh yc-user@<публичный-IP>
   ```

3. **Установи зависимости:**
   ```bash
   sudo apt update && sudo apt install -y python3 python3-pip python3-venv git
   ```

4. **Склонируй проект с GitHub:**
   ```bash
   git clone https://github.com/твой-username/anaprilin-bot.git
   cd anaprilin-bot
   ```

5. **Создай `.env` файл:**
   ```bash
   nano .env
   ```
   
   Вставь:
   ```
   BOT_TOKEN=твой_токен
   TIMEZONE=Europe/Moscow
   REMINDER_TIMES=12:00,17:00,22:00
   DATA_FILE=data/confirmations.json
   ```

6. **Установи зависимости Python:**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```

7. **Создай systemd сервис для автозапуска:**
   ```bash
   sudo nano /etc/systemd/system/anaprilin-bot.service
   ```
   
   Вставь:
   ```ini
   [Unit]
   Description=Anaprilin Reminder Bot
   After=network.target

   [Service]
   Type=simple
   User=yc-user
   WorkingDirectory=/home/yc-user/anaprilin-bot
   Environment="PATH=/home/yc-user/anaprilin-bot/.venv/bin"
   ExecStart=/home/yc-user/anaprilin-bot/.venv/bin/python bot.py
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```

8. **Запусти бота:**
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable anaprilin-bot
   sudo systemctl start anaprilin-bot
   ```

9. **Проверь статус:**
   ```bash
   sudo systemctl status anaprilin-bot
   sudo journalctl -u anaprilin-bot -f
   ```

10. **Готово!** 🎉 Бот работает 24/7. Данные сохраняются в `~/anaprilin-bot/data/`.

### Резервное копирование данных

Настрой автоматическое копирование в Yandex Object Storage:

```bash
# Установи yc CLI
curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash

# Создай скрипт backup.sh
cat > ~/backup.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y-%m-%d)
tar -czf /tmp/anaprilin-backup-$DATE.tar.gz -C ~/anaprilin-bot/data .
yc storage s3api put-object --bucket твой-бакет --key backups/anaprilin-$DATE.tar.gz --body /tmp/anaprilin-backup-$DATE.tar.gz
rm /tmp/anaprilin-backup-$DATE.tar.gz
EOF

chmod +x ~/backup.sh

# Добавь в cron (ежедневно в 3 ночи)
echo "0 3 * * * /home/yc-user/backup.sh" | crontab -
```

---

## Развёртывание на любом другом VPS (long polling)

1. Установите системные зависимости (Ubuntu/Debian):
   ```bash
   sudo apt update && sudo apt install -y python3 python3-venv git
   ```
2. Склонируйте репозиторий на сервер и создайте `.env`.
3. Настройте виртуальное окружение и зависимости:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```
4. Проверьте запуск вручную: `python bot.py`.
5. Для автоматического старта создайте юнит `systemd` `/etc/systemd/system/anaprilin-bot.service`:

   ```
   [Unit]
   Description=Anaprilin reminder bot
   After=network.target

   [Service]
   WorkingDirectory=/opt/anaprilin-bot
   EnvironmentFile=/opt/anaprilin-bot/.env
   ExecStart=/opt/anaprilin-bot/.venv/bin/python bot.py
   Restart=always
   User=bot

   [Install]
   WantedBy=multi-user.target
   ```

   Обновите пути под свою установку, затем выполните `sudo systemctl daemon-reload`, `sudo systemctl enable --now anaprilin-bot`.

## Переменные окружения

| Имя                     | Назначение                                                    |
| ----------------------- | ------------------------------------------------------------- |
| `BOT_TOKEN`             | Telegram Bot API token                                        |
| `TIMEZONE`              | Часовой пояс IANA (например `Europe/Moscow`)                  |
| `REMINDER_TIMES`        | Список времён `HH:MM` через запятую                           |
| `DATA_FILE`             | Путь к JSON с историей подтверждений                          |

**Примечание:** Повторные напоминания «Не забудь ответить!» приходят каждые 10 минут в течение часа после основного уведомления (максимум 6 раз).

## Дополнительно

- Файлы `data/confirmations.json` и `data/subscribers.json` можно периодически копировать в облако для резервного копирования.
- Для изменения количества напоминаний просто обновите `REMINDER_TIMES` в `.env`.
- Если нужна web-hook версия или база данных, можно заменить `storage.py` на подходящую реализацию.
- Все данные (подписки и статистика) сохраняются автоматически и восстанавливаются после перезапуска бота.

